// Public configuration only. Never add secrets, tag numbers or resident data here.
// Activate only after completing backend/README.md with the board and VvE manager.
window.VVE_MELDING_CONFIG = Object.freeze({
  enabled: false,
  endpoint: "",
  turnstileSiteKey: ""
});
